### Function 1: retrieve country names

Function name:
retrieve country names

Core endpoint(s):
- `GET /countries`

Preconditions:
- No database resource state is required. The endpoint reads the configured `country_list.txt` resource through `CountryList`.

Successful execution:
- Result:
  The endpoint returns the configured list of country names accepted by the API's country validator.
- Invocation:
  Step 1: `GET /countries`
- Constraints:
  No path, query, body, form, or header value is required by the controller. Swagger documents generic `401`, `403`, and `404` responses, but the source contains no authentication, authorization, or not-found branch for this endpoint.

Failure or exceptional branches:
- No implementation-defined failure branch is visible in `src`; the controller directly returns `200` with the loaded country list.

Endpoint coverage:
- Covers:
  `GET /countries`
- Distinct meaning:
  Discovery endpoint for valid country names.

### Function 2: create news

Function name:
create news

Core endpoint(s):
- `POST /news`

Preconditions:
- No existing database row is required. The endpoint creates a new `NewsEntity` row itself.

Successful execution:
- Result:
  The endpoint creates a news item with an author id, text, server-generated creation time, country, and generated numeric id.
- Invocation:
  Step 1: `POST /news` with a JSON body containing `authorId`, `text`, and `country`, and omitting `id`, `newsId`, and `creationTime`.
- Constraints:
  `country` must match the configured country list case-insensitively. `authorId` must be non-blank and at most 32 characters. `text` must be non-blank and at most 1024 characters. The implementation returns `201` with the generated id in the response body. Swagger lists both `200` with an integer schema and `201`, while the source returns `201` for success.

Failure or exceptional branches:
- Branch 1:
  - Preconditions:
    - No existing database state is required; the failing request body includes a client-supplied `id` or `newsId`.
  - Failure endpoint:
    `POST /news`
  - Why this fails:
    The implementation rejects a create request with `400` when either `id` or `newsId` is present because ids are generated by the database.
  - Intentionally violated constraints:
    The body includes `id` or `newsId`.

- Branch 2:
  - Preconditions:
    - No existing database state is required; the failing request body includes a client-supplied `creationTime`.
  - Failure endpoint:
    `POST /news`
  - Why this fails:
    The implementation rejects a create request with `400` when `creationTime` is present because creation time is assigned during persistence.
  - Intentionally violated constraints:
    The body includes `creationTime`.

- Branch 3:
  - Preconditions:
    - No existing database state is required; the failing request body omits or nulls at least one required content field.
  - Failure endpoint:
    `POST /news`
  - Why this fails:
    The implementation returns `400` if `authorId`, `text`, or `country` is missing or null.
  - Intentionally violated constraints:
    One or more of `authorId`, `text`, or `country` is omitted or null.

- Branch 4:
  - Preconditions:
    - No existing database state is required; the failing request body includes values that violate entity validation.
  - Failure endpoint:
    `POST /news`
  - Why this fails:
    Repository persistence raises `ConstraintViolationException`, and the endpoint returns `400`.
  - Intentionally violated constraints:
    `country` is not in the configured country list, `authorId` is blank or over 32 characters, or `text` is blank or over 1024 characters.

Endpoint coverage:
- Covers:
  `POST /news`
- Distinct meaning:
  Creates a new server-assigned news resource.

### Function 3: list all news

Function name:
list all news

Core endpoint(s):
- `GET /news`

Preconditions:
- No database resource state is required. The endpoint can return an empty list when no `NewsEntity` rows exist.

Successful execution:
- Result:
  The endpoint returns all currently stored news items.
- Invocation:
  Step 1: `GET /news` with `country` omitted or blank and `authorId` omitted or blank.
- Constraints:
  Both `country` and `authorId` query parameters must be omitted or blank to select the unfiltered repository path. Swagger documents generic `401`, `403`, and `404` responses, but the source contains no authentication, authorization, or not-found branch for this endpoint.

Failure or exceptional branches:
- No implementation-defined failure branch is visible in `src`; the controller returns `200` with the transformed repository result when reached.

Endpoint coverage:
- Covers:
  `GET /news`
- Distinct meaning:
  Collection listing with no filters.

### Function 4: list news by country

Function name:
list news by country

Core endpoint(s):
- `GET /news`

Preconditions:
- No database resource state is required. Matching rows can be created by directly inserting `NewsEntity` rows with `country = {country}` or by calling `POST /news` with a valid JSON body whose `country` field is `{country}` and then using the generated id only if later resource-specific operations need it.

Successful execution:
- Result:
  The endpoint returns stored news items whose `country` field matches the supplied `country` query value.
- Invocation:
  Step 1: `GET /news` with query parameter `country={country}` and `authorId` omitted or blank.
- Constraints:
  `country` must be supplied as a non-blank query parameter. `authorId` must be omitted or blank. The implementation does not validate the query country against `/countries`; unmatched or invalid values produce an empty result set.

Failure or exceptional branches:
- No implementation-defined failure branch is visible in `src`; unmatched or invalid country values simply produce an empty result set.

Endpoint coverage:
- Covers:
  `GET /news`
- Distinct meaning:
  Country-filtered news search.

### Function 5: list news by author

Function name:
list news by author

Core endpoint(s):
- `GET /news`

Preconditions:
- No database resource state is required. Matching rows can be created by directly inserting `NewsEntity` rows with `authorId = {authorId}` or by calling `POST /news` with a valid JSON body whose `authorId` field is `{authorId}` and then using the generated id only if later resource-specific operations need it.

Successful execution:
- Result:
  The endpoint returns stored news items whose `authorId` field matches the supplied `authorId` query value.
- Invocation:
  Step 1: `GET /news` with query parameter `authorId={authorId}` and `country` omitted or blank.
- Constraints:
  `authorId` must be supplied as a non-blank query parameter. `country` must be omitted or blank. Unmatched author ids produce an empty result set.

Failure or exceptional branches:
- No implementation-defined failure branch is visible in `src`; unmatched author ids simply produce an empty result set.

Endpoint coverage:
- Covers:
  `GET /news`
- Distinct meaning:
  Author-filtered news search.

### Function 6: list news by country and author

Function name:
list news by country and author

Core endpoint(s):
- `GET /news`

Preconditions:
- No database resource state is required. Matching rows can be created by directly inserting `NewsEntity` rows with `country = {country}` and `authorId = {authorId}` or by calling `POST /news` with a valid JSON body containing those same values and then using the generated id only if later resource-specific operations need it.

Successful execution:
- Result:
  The endpoint returns stored news items matching both the supplied `country` and `authorId`.
- Invocation:
  Step 1: `GET /news` with query parameters `country={country}` and `authorId={authorId}`.
- Constraints:
  Both `country` and `authorId` must be supplied as non-blank query parameters. The implementation applies both filters together and does not validate the query country against `/countries`; unmatched combinations produce an empty result set.

Failure or exceptional branches:
- No implementation-defined failure branch is visible in `src`; unmatched combinations simply produce an empty result set.

Endpoint coverage:
- Covers:
  `GET /news`
- Distinct meaning:
  Combined country-and-author filtered news search.

### Function 7: retrieve news by id

Function name:
retrieve news by id

Core endpoint(s):
- `GET /news/{id}`

Preconditions:
- A `NewsEntity` row exists with numeric id `{id}`, `authorId`, `text`, `country`, and `creationTime`. This can be satisfied by directly inserting a valid row into the database or by calling `POST /news` with JSON fields `authorId`, `text`, and `country`.
- The `{id}` used by `GET /news/{id}` must identify the existing row. If the API is used to establish the row, `{id}` must be obtained from the `POST /news` `201` response body.

Successful execution:
- Result:
  The endpoint retrieves the news item identified by `{id}` and returns a DTO containing both `newsId` and deprecated `id`.
- Invocation:
  Step 1: `GET /news/{id}` with `{id}` set to the numeric id of an existing news row.
- Constraints:
  `{id}` must parse as `Long` and must correspond to an existing database row.

Failure or exceptional branches:
- Branch 1:
  - Preconditions:
    - No existing database state is required; the failing path value `{id}` is non-numeric.
  - Failure endpoint:
    `GET /news/{id}`
  - Why this fails:
    The implementation parses `{id}` as `Long`; parsing failure returns `404`.
  - Intentionally violated constraints:
    `{id}` is non-numeric.

- Branch 2:
  - Preconditions:
    - No `NewsEntity` row exists with numeric id `{id}`. This can be produced by starting from an empty database, deleting the row beforehand, directly omitting the row, or intentionally not calling `POST /news` for that id.
  - Failure endpoint:
    `GET /news/{id}`
  - Why this fails:
    The repository lookup returns no entity, so the implementation returns `404`.
  - Intentionally violated constraints:
    The required news row state is omitted, or `{id}` is not the id returned by any successful `POST /news` call.

Endpoint coverage:
- Covers:
  `GET /news/{id}`
- Distinct meaning:
  Resource-specific read by generated news id.

### Function 8: replace existing news

Function name:
replace existing news

Core endpoint(s):
- `PUT /news/{id}`

Preconditions:
- A `NewsEntity` row exists with numeric id `{id}`. This can be satisfied by directly inserting a valid row into the database or by calling `POST /news` with JSON fields `authorId`, `text`, and `country`.
- The `{id}` used by `PUT /news/{id}` must identify the existing row. If the API is used to establish the row, `{id}` must be obtained from the `POST /news` `201` response body.

Successful execution:
- Result:
  The endpoint replaces the mutable fields of the existing news item: `text`, `authorId`, `country`, and `creationTime`.
- Invocation:
  Step 1: `PUT /news/{id}` with `{id}` set to the numeric id of an existing news row and a JSON body containing `newsId` or deprecated `id` equal to `{id}`, plus non-null `text`, `authorId`, `country`, and `creationTime`.
- Constraints:
  The body must contain either `newsId` or deprecated `id` equal to `{id}`; if both are supplied, `newsId` is used. `text`, `authorId`, `country`, and `creationTime` must be non-null. `country`, `authorId`, and `text` must satisfy the same persistence validation as creation. Swagger lists `200` and `201`, but the implementation returns `204` on success.

Failure or exceptional branches:
- Branch 1:
  - Preconditions:
    - No existing database state is required; the failing JSON body omits both `newsId` and `id`, or supplies a non-numeric value in the selected id field.
  - Failure endpoint:
    `PUT /news/{id}`
  - Why this fails:
    The implementation reads `newsId` first, falling back to deprecated `id`, then parses it as `Long`. Missing or non-numeric body id returns `404`.
  - Intentionally violated constraints:
    The body id is missing or cannot be parsed as a numeric id.

- Branch 2:
  - Preconditions:
    - A `NewsEntity` row exists with numeric id `{existingId}`. This can be satisfied by directly inserting a valid row or by calling `POST /news` with JSON fields `authorId`, `text`, and `country`.
    - The path id `{id}` uses the existing row id, but the JSON body contains a different `newsId` or deprecated `id` value `{differentId}`.
  - Failure endpoint:
    `PUT /news/{id}`
  - Why this fails:
    The implementation rejects attempts to change the resource id and returns `409` when the selected body id string differs from the path id string.
  - Intentionally violated constraints:
    The body id does not match the path `{id}` for the existing resource.

- Branch 3:
  - Preconditions:
    - No `NewsEntity` row exists with numeric id `{id}`. This can be produced by starting from an empty database, deleting the row beforehand, directly omitting the row, or intentionally not calling `POST /news` for that id.
    - The JSON body still contains `newsId` or deprecated `id` equal to `{id}` so that id parsing and path/body matching pass before the existence check.
  - Failure endpoint:
    `PUT /news/{id}`
  - Why this fails:
    The implementation checks `existsById` and returns `404` when the id is absent. It does not create missing resources via PUT.
  - Intentionally violated constraints:
    The required existing news row state is omitted while the body id matches the path id.

- Branch 4:
  - Preconditions:
    - A `NewsEntity` row exists with numeric id `{id}`. This can be satisfied by directly inserting a valid row into the database or by calling `POST /news` with JSON fields `authorId`, `text`, and `country`.
    - The JSON body contains `newsId` or deprecated `id` equal to `{id}`, but omits or nulls at least one of `text`, `authorId`, `country`, or `creationTime`.
  - Failure endpoint:
    `PUT /news/{id}`
  - Why this fails:
    The implementation returns `400` if `text`, `authorId`, `country`, or `creationTime` is null.
  - Intentionally violated constraints:
    One or more required replacement fields are omitted or null.

- Branch 5:
  - Preconditions:
    - A `NewsEntity` row exists with numeric id `{id}`. This can be satisfied by directly inserting a valid row into the database or by calling `POST /news` with JSON fields `authorId`, `text`, and `country`.
    - The JSON body contains `newsId` or deprecated `id` equal to `{id}`, a non-null `creationTime`, and invalid replacement values for `country`, `authorId`, or `text`.
  - Failure endpoint:
    `PUT /news/{id}`
  - Why this fails:
    Repository update triggers validation and returns `400` on `ConstraintViolationException`.
  - Intentionally violated constraints:
    The body uses an invalid country, blank or overlong `authorId`, or blank or overlong `text`.

Endpoint coverage:
- Covers:
  `PUT /news/{id}`
- Distinct meaning:
  Full replacement of an existing news item; the implementation does not create missing resources via PUT.

### Function 9: update news text

Function name:
update news text

Core endpoint(s):
- `PUT /news/{id}/text`

Preconditions:
- A `NewsEntity` row exists with numeric id `{id}`. This can be satisfied by directly inserting a valid row into the database or by calling `POST /news` with JSON fields `authorId`, `text`, and `country`.
- The `{id}` used by `PUT /news/{id}/text` must identify the existing row. If the API is used to establish the row, `{id}` must be obtained from the `POST /news` `201` response body.

Successful execution:
- Result:
  The endpoint changes only the `text` field of the existing news item.
- Invocation:
  Step 1: `PUT /news/{id}/text` with `{id}` set to the numeric id of an existing news row and a `text/plain` body containing the replacement text.
- Constraints:
  The request body is `text/plain` and becomes the new text. The text must be non-blank and at most 1024 characters. Swagger lists `200` and `201`, but the implementation returns `204` on success.

Failure or exceptional branches:
- Branch 1:
  - Preconditions:
    - No `NewsEntity` row exists with numeric id `{id}`. This can be produced by starting from an empty database, deleting the row beforehand, directly omitting the row, or intentionally not calling `POST /news` for that id.
  - Failure endpoint:
    `PUT /news/{id}/text`
  - Why this fails:
    The implementation checks `existsById` and returns `404` when no entity has that id.
  - Intentionally violated constraints:
    The required existing news row state is omitted.

- Branch 2:
  - Preconditions:
    - A `NewsEntity` row exists with numeric id `{id}`. This can be satisfied by directly inserting a valid row into the database or by calling `POST /news` with JSON fields `authorId`, `text`, and `country`.
    - The plain-text request body is blank or longer than 1024 characters.
  - Failure endpoint:
    `PUT /news/{id}/text`
  - Why this fails:
    The repository update violates the `text` constraints and the endpoint returns `400`.
  - Intentionally violated constraints:
    The replacement text is blank or longer than 1024 characters.

- Branch 3:
  - Preconditions:
    - No existing database state is required; the failing path value `{id}` is non-numeric.
  - Failure endpoint:
    `PUT /news/{id}/text`
  - Why this fails:
    The controller path variable is typed as `Long`; a non-numeric path value cannot satisfy that binding and is rejected as a bad request before the method can update the row.
  - Intentionally violated constraints:
    `{id}` is non-numeric.

Endpoint coverage:
- Covers:
  `PUT /news/{id}/text`
- Distinct meaning:
  Partial update of only the text field.

### Function 10: delete news

Function name:
delete news

Core endpoint(s):
- `DELETE /news/{id}`

Preconditions:
- A `NewsEntity` row exists with numeric id `{id}`. This can be satisfied by directly inserting a valid row into the database or by calling `POST /news` with JSON fields `authorId`, `text`, and `country`.
- The `{id}` used by `DELETE /news/{id}` must identify the existing row. If the API is used to establish the row, `{id}` must be obtained from the `POST /news` `201` response body.

Successful execution:
- Result:
  The endpoint deletes the news item identified by `{id}`.
- Invocation:
  Step 1: `DELETE /news/{id}` with `{id}` set to the numeric id of an existing news row.
- Constraints:
  `{id}` must parse as `Long` and must correspond to an existing database row at deletion time. Swagger lists `204` and a generic `200`, while the implementation returns `204` on success.

Failure or exceptional branches:
- Branch 1:
  - Preconditions:
    - No existing database state is required; the failing path value `{id}` is non-numeric.
  - Failure endpoint:
    `DELETE /news/{id}`
  - Why this fails:
    The implementation parses `{id}` as `Long`; parsing failure returns `400`.
  - Intentionally violated constraints:
    `{id}` is non-numeric.

- Branch 2:
  - Preconditions:
    - A `NewsEntity` row initially exists with numeric id `{id}`. This can be satisfied by directly inserting a valid row into the database or by calling `POST /news` with JSON fields `authorId`, `text`, and `country`.
    - Before the failing request, that row no longer exists because it was removed from the database directly or because `DELETE /news/{id}` was called once with the same id.
  - Failure endpoint:
    `DELETE /news/{id}`
  - Why this fails:
    The implementation checks `existsById` before deleting. Reusing an id after the row has already been removed returns `404`. Swagger does not document this `404`, but the source does.
  - Intentionally violated constraints:
    The failing request attempts to delete an id whose resource state was removed before the request.

Endpoint coverage:
- Covers:
  `DELETE /news/{id}`
- Distinct meaning:
  Deletes an existing generated news resource.
